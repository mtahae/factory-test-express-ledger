{
  "name": "ledger-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ledger API - a small expense tracking service.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
